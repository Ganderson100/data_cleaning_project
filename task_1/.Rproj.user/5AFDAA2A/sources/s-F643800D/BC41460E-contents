---
title: "R Notebook"
output: html_notebook
---
```{r}
library(readr)
library(tidyverse)
```

```{r}
library(here)

#test where the top level of the project directory is 
here::here()

#use this to set the path to the data file
dirty_decathlon <- read_rds(here("raw_data/decathlon.rds"))
```


```{r}
dim(dirty_decathlon)
head(dirty_decathlon, 10)
tail(dirty_decathlon, 10)
view(dirty_decathlon)
```

```{r}
names(dirty_decathlon)
```

```{r}
# rename column headings to all lower case with no spaces
library(janitor)

dirty_decathlon2 <- dirty_decathlon
dirty_decathlon2 <- clean_names(dirty_decathlon2)
names(dirty_decathlon2)
```

```{r}
# create a "athlete_name" column 
dirty_decathlon3 <- dirty_decathlon2 %>%
  rownames_to_column("athlete_name")
names(dirty_decathlon3)

```

```{r}
# setting data in the name column to lower case
library(stringr)
dirty_decathlon4 <- dirty_decathlon3 %>%
  mutate(athlete_name = str_to_lower(athlete_name))
dirty_decathlon4
```

```{r}
# Could have changed the events into long format but after reviewing the analysis required I decided not to as it didnt seem necessary to answer the questions.
```

```{r}
view(dirty_decathlon4)
```

```{r}
here::here()
write_csv(dirty_decathlon4, here("clean_data/clean_decathlon.csv"))
```

